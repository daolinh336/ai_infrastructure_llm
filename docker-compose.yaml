services:
  nginx:
    image: nginx:stable
    restart: unless-stopped
    deploy:
      replicas: 1
    ports:
      - 80:80
    environment: {}
    depends_on:
      - api
    networks:
      - test-app-network
  api:
    image: python:latest
    restart: unless-stopped
    deploy:
      replicas: 2
    environment: {}
    depends_on:
      - postgres
    command:
      - tail
      - -f
      - /dev/null
    networks:
      - test-app-network
  postgres:
    image: postgres:16
    restart: unless-stopped
    deploy:
      replicas: 1
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: abcabcabc
      POSTGRES_PASSWORD: pw-9ca6cf2b60296a25cf6781af
    volumes:
      - test-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U abcabcabc
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - test-app-network
networks:
  test-app-network: {}
volumes:
  test-postgres-data: {}
