services:
  nginx:
    image: nginx:stable
    restart: unless-stopped
    deploy:
      replicas: 1
    ports:
      - 80:80
    environment: {}
    depends_on:
      - api
    networks:
      - test2-app-network
  api:
    image: node:20-alpine
    restart: unless-stopped
    deploy:
      replicas: 2
    environment: {}
    depends_on:
      - postgres
    command:
      - tail
      - -f
      - /dev/null
    networks:
      - test2-app-network
  postgres:
    image: postgres:16
    restart: unless-stopped
    deploy:
      replicas: 1
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: abcabcabc
      POSTGRES_PASSWORD: pw-9c9cf28dbf573fc975cb9e21
    volumes:
      - test2-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U app
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - test2-app-network
networks:
  test2-app-network: {}
volumes:
  test2-postgres-data: {}
